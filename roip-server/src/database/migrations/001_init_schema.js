/**
 * Initial Schema Migration
 * Creates all base tables for the RoIP server
 */

export default async function migration(db) {
  // This migration is handled by the schema creation in database.js
  // This file serves as a placeholder for future schema modifications
  console.log('Initial schema already created by DatabaseModule.createSchema()');
}
